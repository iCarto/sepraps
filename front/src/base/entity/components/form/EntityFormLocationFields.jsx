import {FormMapGeojsonPoint} from "base/form/components";
// import {useEntityLayer} from "base/entity/map";

const EntityFormLocationFields = ({rules = {}}) => {
    // return <FormMapGeojsonPoint name="geom" layer={useEntityLayer} rules={rules} />;
};

export default EntityFormLocationFields;
