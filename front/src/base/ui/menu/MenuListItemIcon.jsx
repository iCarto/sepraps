import ListItemIcon from "@mui/material/ListItemIcon";

const MenuListItemIcon = ({children}) => {
    return <ListItemIcon style={{minWidth: "35px"}}>{children}</ListItemIcon>;
};

export default MenuListItemIcon;
